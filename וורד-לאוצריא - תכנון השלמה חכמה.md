# השלמה חכמה: תיוג מקורות, ביטויים תלמודיים, מחברים וראשי תיבות

מסמך תכנון לפני מימוש. נובע משרשור בפורום (mitmachim.top/post/1238414) שבו יאיר
דניאל, י.-פל. ונתנאל26 דנו בבקשת משתמש לתיוג אוטומטי של מקורות תוך כדי הקלדה.
הוחלט **לא** לטעון את הספרייה לזיכרון — פתרון משולב: RPC עצל לתיוג מקורות
מדויקים, ומילונים סטטיים קטנים למה שאין לו הצדקה ל-RPC.

## מצב קיים (לא לגעת בהתנהגות שלו)

`src/engine/book-completion.ts` + `book-completion-overlay.ts`: "השלמה מהספר" —
משלימה טקסט מהספר **הפתוח כרגע בקורא** (תוכן + שם ספר), ghost text אפור,
קבלה ב-Tab. state machine: `{kind:'idle'} | {kind:'suggesting', ...}`.

## תכונה 1 — תיוג מקורות עם `@`, המרה אוטומטית לקישור

**טריגר**: token פתוח שמתחיל ב-`@` (מ-`@` האחרון ועד רווח/סוף פסקה, בלי רווח
באמצע). כל עוד ה-token פתוח — **מדלגים לגמרי** על "השלמה מהספר" (גם השלמת
תוכן וגם השלמת שם הספר הפתוח) לאותה הקשה. זו התנגשות שנמדדה מראש: שתי
המערכות מאזינות לאותו `input`/`keyup` ותופסות את אותו `Tab`.

**זרימה**:
1. `@יומא` — התאמת שם ספר מתוך cache עצלה של `library.getTree` (נטענת בפעם
   הראשונה שמקלידים `@`, לא בעליית התוסף). הרשאה: `library.books.read`.
2. זיהוי ספר → `library.getBookMetadata({bookId})` בשביל ה-`id` המספרי
   (שונה מ-`bookId` המחרוזתי — נדרש ל-URL).
3. `library.getBookToc({bookId})` — עצלה, פר-ספר, cache. הרשאה:
   `library.content.read`.
4. פענוח מה שהוקלד אחרי שם הספר (`" יג:"`): **המרה מותרת רק עם סימון עמוד
   מפורש** — `.`/`:`, או `א'`/`ב'`, או `ע"א`/`ע"ב`. "פסחים יג" בלי סימן —
   **אין המרה**. נורמליזציה לצורה הקנונית (`"דף יג:"`, בדיוק כפי שנמדד ב-
   `tocEntry` של אוצריא עצמה — `seforim.db`, ספר יומא: `'דף יג.'`/`'דף יג:'`,
   `level:1`) והתאמה למפה שנבנתה מ-`getBookToc`.
5. התאמה → `index` של הערך. בניית `href = "otzaria://open/book/<id numeric>?index=<n>"`.
6. **בסגירת ה-token** (רווח/סוף פסקה אחרי סימון עמוד תקין) — עטיפה אוטומטית
   כהיפר-קישור אמיתי: `doc.hyperlinks.wrap({target: TextAddress, link:
   {destination:{href}}})` (כבר קיים ונבדק ב-`hyperlinks-manage.ts`). ברירת
   מחדל: **דלוק**, לא כפתור נפרד — המרה חיה תוך כדי הקלדה.
7. ספר בלי "דף" ברמת ה-toc (שו"ע וכו') — אין התאמה, אין שגיאה, נשאר טקסט רגיל.

**קבצים**: `public/manifest.json` (2 הרשאות חדשות) ·
`src/host/otzaria-library.ts` (חדש: עטיפות RPC + cache) ·
`src/engine/source-tagging.ts` (חדש: פרסר + matcher, טהור, נבדק ביחידה) ·
`src/engine/book-completion-overlay.ts` (שינוי: state נוסף `'tagging'`,
דילוג על ההשלמות הקיימות כש-token פתוח).

## תכונה 2 — מילון סטטי: ביטויים תלמודיים + מחברים

לא RPC, לא נטען מהספרייה — נכס נפרד שנטען עצל בהזרקת `<script>`, כמו המילון
התורני וראשי-התיבות (`import` ישיר הכניס את התוכן ל-`assets/app.js` ואחד
הביטויים התנגש בטעות עם סמן ה-`check:dist` של המילון התורני — תוקן).

**מקורות שנאספו בפועל (לא מנוסחים על ידי):**
- **ביטויים** (964 סה"כ, מוזגו משלושה מקורות, `src/data/talmudic-phrases.json`):
  - קטגוריית ויקימילון "[ניבים, ביטויים ופתגמים מהתלמוד
    ומהמשנה](https://he.wiktionary.org/wiki/קטגוריה:ניבים,_ביטויים_ופתגמים_מהתלמוד_ומהמשנה)"
    — 325 ערכים (326 פחות template artifact), נמשכו דרך ה-API הציבורי של
    ויקימדיה. רישיון: CC BY-SA + GFDL (ויקימדיה).
  - כותרות מרחב השם `אנציקלופדיה_תלמודית:` ב-`wikishiva.zim` (מופע ZIM מקומי
    של ויקישיבה, `yeshiva.org.il/wiki`) — 629 ערכים (מונחים הלכתיים/תלמודיים
    ממשית, למשל "מצוה הבאה בעבירה", "מקח טעות" — קרוב יותר לדוגמה "מים
    אחרונים חובה" מהדיון המקורי מאשר ניבי ויקימילון).
  - כותרות מהדף "מונחים שכיחים בתלמוד" באותו ZIM — 13 מונחי דיאלקטיקה
    (פשיטא, תיקו, צריכא, תא שמע, איתמר, ורמינהו ועוד).
  - **⚠️ שאלת רישיון פתוחה, לא הוכרעה כאן**: העמודים ב-wikishiva מוצהרים
    בפוטר כ-**CC BY-NC 3.0** (Attribution-**Noncommercial**) — רישיון שונה
    מזה של ויקימילון. מה שנלקח הוא **רק כותרות הערכים** (מחרוזת קצרה לכל
    אחד), לא תוכן הפרוזה ההסברי — וכותרות/מונחים בני מילים בודדות נחשבים
    בדרך כלל עובדתיים ואינם מוגנים בזכויות יוצרים ברוב השיטות המשפטיות, אבל
    זו עמדה שדורשת אישור מפורש של בעלי המאגר לפני מיזוג, לא הכרעה חד-צדדית
    כאן — בדיוק כמו האופן שבו `THIRD_PARTY_NOTICES.md` מטפל באייקוני
    Fluent. **סומן במפורש כתגובה על ה-PR עצמו.**
  - מקורות נוספים שנמצאו באותו מיזם (ZIM) ולא נוצלו: `milon-klei-kodesh.zim`
    (מילון מעורב — מונחים בודדים, ראשי תיבות, **שמות מחברים** גם יחד — לא
    "ביטויים" בעיקרו) ו-`wikisugya.zim` (ויקיסוגיה, לימוד סוגיות) — לא
    נסרקו.
- **מחברים**: טבלת `author` ב-`seforim.db` של אוצריא — 651 שמות מהמופע
  המקומי שנבדק. **לא בהכרח הרשימה המלאה** של ספריית ברירת המחדל המלאה —
  להרחיב בהמשך (מקור פוטנציאלי: `Sefaria-Export`, או `milon-klei-kodesh.zim`
  שנמצא בו לפחות מדגם של שמות מחברים — דורש מיפוי/סינון, לא בוצע).

**מנגנון**: מודול matching גנרי (לא כפילות ללוגיקה של `book-completion.ts` —
אותו רעיון של "הקשר + מילה חלקית → השלמה", אבל המקור הוא רשימה סטטית ולא
טקסט חי מהקורא). מופעל **רק כשההשלמה מהספר לא מצאה התאמה** לאותה הקשה —
כך אין שינוי בסדר העדיפויות הקיים והנבדק.

**קבצים**: `src/data/talmudic-phrases.json`, `src/data/authors.json` ·
`src/engine/static-completion.ts` (matcher + טעינה עצלה משותפת) ·
`book-completion-overlay.ts` (fallback אחרי שההשלמה מהספר נכשלה).

## תכונה 3 — השלמת ראשי תיבות

מקור: `Acronyms.json` של אוצריא עצמה (13,105 ערכים, `ר"ת → [פירוש, פירוש, ...]`)
— לא מקור חיצוני, זה כבר קובץ הנתונים הרשמי של אוצריא. **הבדל מבני**: לכל
ר"ת כמה פירושים אפשריים (לא תוצאה יחידה כמו שתי התכונות הקודמות), ולכן ghost
text יחיד לא מספיק. **החלטת UX למימוש ראשון**: מציגים את הפירוש הראשון
כ-ghost text רגיל (קבלה ב-Tab, כמו כל השלמה אחרת כאן); הרחבה עתידית — מחזור
בין אפשרויות בלחיצות Tab חוזרות. לא נבנה dropdown נפרד בשלב הזה.

**קבצים**: `src/data/acronyms.json` (13,105 ערכים, נכס נפרד שנטען עצל) ·
`src/engine/acronyms.ts` + `acronym-dictionary.ts`.

## סדר עדיפויות סופי בכל הקשה (evaluate יחיד ב-overlay)

1. `@` token פתוח → תיוג מקורות בלבד (חוסם הכול אחר).
2. אחרת: השלמה מהספר הפתוח בקורא (קיים, לא משתנה) — שם ספר, ואז תוכן.
3. אחרת: מילון סטטי — ביטויים, מחברים, ראשי תיבות (בסדר הזה).
4. אין התאמה בשום שכבה → ללא ghost, כמו היום.

## בדיקות

- `source-tagging.ts`: יחידה — התאמות חיוביות/שליליות (כולל "אין סימן עמוד
  → אין המרה"), נורמליזציה לצורת ה-toc. **28/28 עוברות.**
- `static-completion.ts`: יחידה — טעינה עצלה + fallback רק כשאין התאמה
  מהספר. **5/5 עוברות.**
- `acronyms.ts`: יחידה — פענוח + טעינה עצלה. **4/4 עוברות.**
- `book-completion-overlay.ts`: אינטגרציה — אין שני ghost בו-זמנית, `@`
  שולל את שתי צורות ההשלמה הקיימות, סדר העדיפויות נשמר (לא נבדק מול מנוע
  אמיתי — אין סביבת Windows/CDP זמינה).
- `manifest.test.ts`: שתי ההרשאות החדשות מוצהרות ותואמות למה שבאמת נצרך.

## מה לא בסקופ הנוכחי

- הרחבת רשימת המחברים מעבר ל-651 (Sefaria-Export, `milon-klei-kodesh.zim`).
- `wikisugya.zim`, `otzar-hasfarim.zim` — לא נוצלו.
- מחזור בין כמה פירושי ר"ת (רק פירוש ראשון בשלב זה).

## סטטוס: הושלם ונדחף

**PR פתוח**: https://github.com/Y-PLONI/otzaria-word-editor/pull/46
(ענף `feat/smart-source-completion`, מהפורק `YairDaniel11/otzaria-word-editor`
— אין הרשאת push ישירה ל-`Y-PLONI/otzaria-word-editor`).

- `npx vue-tsc --noEmit` — נקי.
- `npm run build` + `node scripts/check-dist.mjs` — ירוקים.
- כל `tests/unit`/`contract`/`component` עוברים (14 כשלים קיימים מראש ב-main,
  לא קשורים — פער סביבתי Node 18/20).
- **תגובה על ה-PR** מסמנת במפורש את שאלת הרישיון (CC BY-NC) של תוכן ה-ZIM,
  לאישור בעלי המאגר לפני מיזוג.
- לא נבדק מול מנוע אמיתי (Windows/CDP) — מסומן בגוף ה-PR.
